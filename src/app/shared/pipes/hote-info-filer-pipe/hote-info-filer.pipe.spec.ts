import { HoteInfoFilerPipe } from './hote-info-filer.pipe';

describe('HoteInfoFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new HoteInfoFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
